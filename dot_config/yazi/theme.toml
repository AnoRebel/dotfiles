# Auto-generated by awww_generate_themes.sh from pywal colors
# Source wallpaper: /home/ano/Pictures/hyprwallpapers/background.png
# DO NOT EDIT — will be overwritten on next wallpaper change.

# ── Flavor (disabled — using dynamic pywal colors) ───────────────────
# [flavor]
# dark = "tokyo-night"

# ── App ──────────────────────────────────────────────────────────────
[app]
overall = { bg = "#0b0708" }

# ── Manager ──────────────────────────────────────────────────────────
[mgr]
cwd              = { fg = "#157678", italic = true }
find_keyword     = { fg = "#3B5D63", bold = true }
find_position    = { fg = "#2F676C", italic = true }
symlink_target   = { fg = "#454F55", italic = true }
marker_copied    = { fg = "#354A4F", bg = "#354A4F" }
marker_cut       = { fg = "#185052", bg = "#185052" }
marker_marked    = { fg = "#2F676C", bg = "#2F676C" }
marker_selected  = { fg = "#157678", bg = "#157678" }
count_copied     = { fg = "#0b0708", bg = "#354A4F" }
count_cut        = { fg = "#0b0708", bg = "#185052" }
count_selected   = { fg = "#0b0708", bg = "#157678" }
border_symbol    = "│"
border_style     = { fg = "#697374" }

# ── Indicator ────────────────────────────────────────────────────────
[indicator]
parent  = { fg = "#454F55" }
current = { fg = "#157678" }
preview = { fg = "#697374" }

# ── Tabs ─────────────────────────────────────────────────────────────
[tabs]
active   = { fg = "#0b0708", bg = "#157678", bold = true }
inactive = { fg = "#97a5a7", bg = "#697374" }

# ── Mode ─────────────────────────────────────────────────────────────
[mode]
normal_main = { fg = "#0b0708", bg = "#157678", bold = true }
normal_alt  = { fg = "#157678", bg = "#0b0708" }
select_main = { fg = "#0b0708", bg = "#354A4F", bold = true }
select_alt  = { fg = "#354A4F", bg = "#0b0708" }
unset_main  = { fg = "#0b0708", bg = "#2F676C", bold = true }
unset_alt   = { fg = "#2F676C", bg = "#0b0708" }

# ── Status bar ───────────────────────────────────────────────────────
[status]
overall        = { fg = "#97a5a7", bg = "#0b0708" }
perm_type      = { fg = "#157678" }
perm_read      = { fg = "#354A4F" }
perm_write     = { fg = "#185052" }
perm_exec      = { fg = "#3B5D63" }
perm_sep       = { fg = "#697374" }
progress_label = { fg = "#97a5a7", bold = true }
progress_normal = { fg = "#157678", bg = "#0b0708" }
progress_error  = { fg = "#185052", bg = "#0b0708" }

# ── Which key popup ──────────────────────────────────────────────────
[which]
cols            = 3
mask            = { bg = "#0b0708" }
cand            = { fg = "#157678" }
rest            = { fg = "#697374" }
desc            = { fg = "#97a5a7" }
separator       = " → "
separator_style = { fg = "#697374" }

# ── Confirm dialog ───────────────────────────────────────────────────
[confirm]
border  = { fg = "#157678" }
title   = { fg = "#157678", bold = true }
body    = { fg = "#97a5a7" }
list    = { fg = "#97a5a7" }
btn_yes = { fg = "#0b0708", bg = "#157678" }
btn_no  = { fg = "#97a5a7", bg = "#697374" }

# ── Spot (table view) ───────────────────────────────────────────────
[spot]
border   = { fg = "#157678" }
title    = { fg = "#157678", bold = true }
tbl_col  = { fg = "#2F676C" }
tbl_cell = { fg = "#97a5a7" }

# ── Notifications ────────────────────────────────────────────────────
[notify]
title_info  = { fg = "#157678" }
title_warn  = { fg = "#3B5D63" }
title_error = { fg = "#185052" }

# ── Pick (selection popup) ───────────────────────────────────────────
[pick]
border   = { fg = "#157678" }
active   = { fg = "#0b0708", bg = "#157678" }
inactive = { fg = "#97a5a7" }

# ── Input ────────────────────────────────────────────────────────────
[input]
border   = { fg = "#157678" }
title    = { fg = "#157678", bold = true }
value    = { fg = "#97a5a7" }
selected = { fg = "#0b0708", bg = "#157678" }

# ── Completion ───────────────────────────────────────────────────────
[cmp]
border       = { fg = "#157678" }
active       = { fg = "#0b0708", bg = "#157678" }
inactive     = { fg = "#97a5a7" }
icon_file    = ""
icon_folder  = ""
icon_command = ""

# ── Tasks ────────────────────────────────────────────────────────────
[tasks]
border  = { fg = "#157678" }
title   = { fg = "#157678", bold = true }
hovered = { fg = "#0b0708", bg = "#157678" }

# ── Help ─────────────────────────────────────────────────────────────
[help]
on      = { fg = "#157678" }
run     = { fg = "#2F676C" }
desc    = { fg = "#97a5a7" }
hovered = { fg = "#0b0708", bg = "#157678" }
footer  = { fg = "#697374" }

# ── File types ───────────────────────────────────────────────────────
[filetype]
rules = [
    # Images
    { mime = "image/*",               fg = "#3B5D63" },
    # Audio / Video
    { mime = "{audio,video}/*",       fg = "#2F676C" },
    # Archives
    { mime = "application/{*zip,tar,bzip*,7z*,rar,xz}", fg = "#185052" },
    # Documents
    { mime = "application/pdf",       fg = "#354A4F" },
    { mime = "text/*",                fg = "#97a5a7" },
    # Empty files
    { mime = "inode/empty",           fg = "#697374" },
    # Orphan symlinks
    { url = "*", is = "orphan",       fg = "#185052" },
    # Symlinks
    { url = "*", is = "link",         fg = "#454F55", italic = true },
    # Executables
    { url = "*", is = "exec",         fg = "#354A4F" },
    # Directories
    { url = "*/",                     fg = "#157678", bold = true },
]
