# Auto-generated by awww_generate_themes.sh from pywal colors
# Source wallpaper: /home/ano/Pictures/hyprwallpapers/wallhaven-6kvzxl_1920x1080.png
# DO NOT EDIT — will be overwritten on next wallpaper change.

# ── Flavor (disabled — using dynamic pywal colors) ───────────────────
# [flavor]
# dark = "tokyo-night"

# ── App ──────────────────────────────────────────────────────────────
[app]
overall = { bg = "#0D0D16" }

# ── Manager ──────────────────────────────────────────────────────────
[mgr]
cwd              = { fg = "#828B2E", italic = true }
find_keyword     = { fg = "#503963", bold = true }
find_position    = { fg = "#383B84", italic = true }
symlink_target   = { fg = "#3E4187", italic = true }
marker_copied    = { fg = "#2E316E", bg = "#2E316E" }
marker_cut       = { fg = "#1F2964", bg = "#1F2964" }
marker_marked    = { fg = "#383B84", bg = "#383B84" }
marker_selected  = { fg = "#828B2E", bg = "#828B2E" }
count_copied     = { fg = "#0D0D16", bg = "#2E316E" }
count_cut        = { fg = "#0D0D16", bg = "#1F2964" }
count_selected   = { fg = "#0D0D16", bg = "#828B2E" }
border_symbol    = "│"
border_style     = { fg = "#6b6b83" }

# ── Indicator ────────────────────────────────────────────────────────
[indicator]
parent  = { fg = "#3E4187" }
current = { fg = "#828B2E" }
preview = { fg = "#6b6b83" }

# ── Tabs ─────────────────────────────────────────────────────────────
[tabs]
active   = { fg = "#0D0D16", bg = "#828B2E", bold = true }
inactive = { fg = "#999abc", bg = "#6b6b83" }

# ── Mode ─────────────────────────────────────────────────────────────
[mode]
normal_main = { fg = "#0D0D16", bg = "#828B2E", bold = true }
normal_alt  = { fg = "#828B2E", bg = "#0D0D16" }
select_main = { fg = "#0D0D16", bg = "#2E316E", bold = true }
select_alt  = { fg = "#2E316E", bg = "#0D0D16" }
unset_main  = { fg = "#0D0D16", bg = "#383B84", bold = true }
unset_alt   = { fg = "#383B84", bg = "#0D0D16" }

# ── Status bar ───────────────────────────────────────────────────────
[status]
overall        = { fg = "#999abc", bg = "#0D0D16" }
perm_type      = { fg = "#828B2E" }
perm_read      = { fg = "#2E316E" }
perm_write     = { fg = "#1F2964" }
perm_exec      = { fg = "#503963" }
perm_sep       = { fg = "#6b6b83" }
progress_label = { fg = "#999abc", bold = true }
progress_normal = { fg = "#828B2E", bg = "#0D0D16" }
progress_error  = { fg = "#1F2964", bg = "#0D0D16" }

# ── Which key popup ──────────────────────────────────────────────────
[which]
cols            = 3
mask            = { bg = "#0D0D16" }
cand            = { fg = "#828B2E" }
rest            = { fg = "#6b6b83" }
desc            = { fg = "#999abc" }
separator       = " → "
separator_style = { fg = "#6b6b83" }

# ── Confirm dialog ───────────────────────────────────────────────────
[confirm]
border  = { fg = "#828B2E" }
title   = { fg = "#828B2E", bold = true }
body    = { fg = "#999abc" }
list    = { fg = "#999abc" }
btn_yes = { fg = "#0D0D16", bg = "#828B2E" }
btn_no  = { fg = "#999abc", bg = "#6b6b83" }

# ── Spot (table view) ───────────────────────────────────────────────
[spot]
border   = { fg = "#828B2E" }
title    = { fg = "#828B2E", bold = true }
tbl_col  = { fg = "#383B84" }
tbl_cell = { fg = "#999abc" }

# ── Notifications ────────────────────────────────────────────────────
[notify]
title_info  = { fg = "#828B2E" }
title_warn  = { fg = "#503963" }
title_error = { fg = "#1F2964" }

# ── Pick (selection popup) ───────────────────────────────────────────
[pick]
border   = { fg = "#828B2E" }
active   = { fg = "#0D0D16", bg = "#828B2E" }
inactive = { fg = "#999abc" }

# ── Input ────────────────────────────────────────────────────────────
[input]
border   = { fg = "#828B2E" }
title    = { fg = "#828B2E", bold = true }
value    = { fg = "#999abc" }
selected = { fg = "#0D0D16", bg = "#828B2E" }

# ── Completion ───────────────────────────────────────────────────────
[cmp]
border       = { fg = "#828B2E" }
active       = { fg = "#0D0D16", bg = "#828B2E" }
inactive     = { fg = "#999abc" }
icon_file    = ""
icon_folder  = ""
icon_command = ""

# ── Tasks ────────────────────────────────────────────────────────────
[tasks]
border  = { fg = "#828B2E" }
title   = { fg = "#828B2E", bold = true }
hovered = { fg = "#0D0D16", bg = "#828B2E" }

# ── Help ─────────────────────────────────────────────────────────────
[help]
on      = { fg = "#828B2E" }
run     = { fg = "#383B84" }
desc    = { fg = "#999abc" }
hovered = { fg = "#0D0D16", bg = "#828B2E" }
footer  = { fg = "#6b6b83" }

# ── File types ───────────────────────────────────────────────────────
[filetype]
rules = [
    # Images
    { mime = "image/*",               fg = "#503963" },
    # Audio / Video
    { mime = "{audio,video}/*",       fg = "#383B84" },
    # Archives
    { mime = "application/{*zip,tar,bzip*,7z*,rar,xz}", fg = "#1F2964" },
    # Documents
    { mime = "application/pdf",       fg = "#2E316E" },
    { mime = "text/*",                fg = "#999abc" },
    # Empty files
    { mime = "inode/empty",           fg = "#6b6b83" },
    # Orphan symlinks
    { url = "*", is = "orphan",       fg = "#1F2964" },
    # Symlinks
    { url = "*", is = "link",         fg = "#3E4187", italic = true },
    # Executables
    { url = "*", is = "exec",         fg = "#2E316E" },
    # Directories
    { url = "*/",                     fg = "#828B2E", bold = true },
]
